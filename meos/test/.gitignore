# Smoke suites are generated by gen_smoketest.py (from the installed headers +
# meos/test/smoke/*.json sidecars) and regenerated in CI — never committed.
# Hand-written test programs use *_test.c and ARE tracked.
*_smoketest.c
/build/
